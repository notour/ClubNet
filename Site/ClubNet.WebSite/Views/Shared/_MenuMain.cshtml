@using ClubNet.WebSite.BusinessLayer.Contracts;

@inject IMenuBL _menuBL;
@{ 
}

<ul class="nav navbar-nav">

    @foreach (var menuItem in await _menuBL.GetMenuAsync("Main"))
    {
        @await Html.PartialAsync("_MenuItem", menuItem);
    }
</ul>